pipeline {
    agent {
        docker { image 'node:16-alpine' }  // Using the node:16-alpine Docker image
    }
    stages {
        stage('Test') {
            steps {
                script {
                    // Run the command to verify the Node.js version inside the container
                    sh 'node --version'
                }
            }
        }
    }
}
